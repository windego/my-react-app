import React from 'react';

// import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';

interface Props {}

const Root = (props: Props) => {
  // return (
  //   <Routes>
  //     <Route path="/" element={<Home />} />
  //     <Route path="profile/*" element={<Profile />} />
  //   </Routes>
  // );]
  return <div>的点点滴滴</div>;
};

export default Root;
